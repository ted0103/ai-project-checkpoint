interface:
  display_name: "Project Checkpoint"
  short_description: "Save portable, integrity-checked project state"
  default_prompt: "Use $project-checkpoint to save or resume this Git project, including a portable source bundle."
